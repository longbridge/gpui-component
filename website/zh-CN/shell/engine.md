---
title: 引擎接缝
description: 默认 QuickJS、可编译的 LuaJIT 退路、接缝存在的理由，以及决定二者取舍的那次实测。
order: 8
---

# 引擎接缝

脚本引擎位于一条内部接口之后。接缝之上的一切——元素描述 arena、物化、CallScope、样式表、主题、能力模型、浮层宿主、热重载——都与引擎无关，只有引擎模块知道脚本值长什么样。

```bash
# 默认：QuickJS，经由 rquickjs。
cargo run -p gpui-shell -- examples/js_todolist

# 退路，它仍然能编译、也仍然能运行。
cargo run -p gpui-shell --no-default-features --features luajit -- path/to/app
cargo run -p gpui-shell --no-default-features --features lua -- path/to/app
```

只能启用一个引擎。同时启用两个是**编译错误**，而不是静默回落到默认——因为两者导出同名类型，`gpui_shell::ShellRuntime` 会有歧义。feature 组合错了应当在构建期报出来，并附上修正命令。

## 为什么会有这条接缝

引擎选择是这个运行时里唯一无法在纸面上判定的决定。

其余的设计都可以从 GPUI 的元素模型推出来，在白板上就能争清楚。引擎不行，因为整套方案的成立与否取决于一个数字：**脚本代码描述一个真实界面需要多久。** 脚本处在每帧重建元素树的路径上，而 builder 链上的每一次方法调用都是一次跨语言边界。如果这笔单次调用成本过高，任何设计都救不回来。

所以接缝是一种"不必提前判断正确"的做法。决定交给实测，而推翻它是改一个 cargo feature，不是重写。

JavaScript 是默认，理由只有一条，而且是产品理由不是技术理由：**应用代码用它写出来更好读。** 呈现权在脚本手上，应用的绝大部分就是组合元素、写样式、处理事件——这类代码的可读性直接决定这个运行时值不值得用。类、箭头函数、模板字符串与解构，恰好落在这类代码上。附带收益是 JavaScript 在模型训练语料里覆盖最好，这对[三类使用者](./index.md#适合谁)之一是决定性的。

代价也如实写出来：QuickJS 比 LuaJIT 大，而且**没有 JIT**——它是字节码解释器，热点循环与单次调用成本原则上都赢不了 LuaJIT。

## 那次实测

基准测试描述一个 40 × 5 的样式化单元格网格——443 个描述节点，每个约十次记录操作——重复五十次，报告每次的平均耗时。它是决定引擎的唯一一项测试，所以两个引擎都要跑：

```bash
cargo test -p gpui-shell --release --test benchmark -- --nocapture

cargo test -p gpui-shell --release --no-default-features --features luajit \
    --test benchmark -- --nocapture
```

| 引擎 | 443 个节点，每次描述耗时 |
| --- | --- |
| **QuickJS** | **1.14 ms** |
| LuaJIT | 1.36 ms |

在另一台机器上重跑得到 1.09 ms 与 1.43 ms——绝对值随机器变化，相对次序没有变。两组都是 release 构建的数字；不用 release 跑出来的数字没有意义。

值得点名的意外是：**QuickJS 反而领先**，尽管它没有 JIT，尽管 LuaJIT 在热点数值循环上是快得多的解释器。这个负载不是热点循环。它是成千上万次短调用，每一次都跨进 Rust 记录一条操作；在这种形态上，两个引擎接近到 JIT 根本没有机会起作用。

把结果对照设计给出的预算也值得一做，答案是"在预算之内，但余量比预期少"。目标是连续交互期间脚本渲染 1.5 ms，这个规模的面板在两个引擎上都达标。但那份预算是按 800 个节点、每次记录操作约 150 ns 推出来的，而基准报告的是 QuickJS 约 250 ns、LuaJIT 约 320 ns。三倍规模的面板放不进去。设计为此列出的三条手段——压低单次调用成本、对未变化的子树做记忆化、对长列表做虚拟化——恰好都[还没有实现](./elements.md#还没有的东西)。

同一次实测还带来了两个实现选择，今天在运行时里看得到：

- **元素是共用同一个原型的普通对象**，样式方法由一段 JavaScript prelude 遍历名字列表装到该原型上。不是每个元素一个 class，不是每次属性访问新建闭包，也不是 3000 个 Rust 闭包。
- **带诊断的 `Proxy` 原型不是默认。** 用 `Proxy` 包住原型以便说出拼错的方法名，代价是整个描述过程的约 30%；所以运行时保留普通原型，只在一次渲染失败后用诊断原型重跑一遍，纯粹为了产出那条信息。见 [样式](./styling.md#未知方法)。

## 接缝两侧各有什么

这个比例本身就是接缝存在的论据：上面是真正的设计，下面是"脚本值长什么样"。

| 接缝之上——两个引擎共用 | 接缝之下——每个引擎各写一份 |
| --- | --- |
| 元素描述 arena、一次性检查与调试树 | 把引擎值转换成运行时的中立值类型 |
| 物化：描述变成真实 GPUI 元素，纯 Rust | 模块系统的形态——ES module 加解析器，还是 `require` 加路径表 |
| CallScope：phase、代次，以及整个 crate 唯一的 `unsafe` | 方法派发——共享原型上的函数，还是 `__index` 元方法 |
| 样式表、有参样式与拼写建议 | 回调句柄类型 |
| 默认 token 调色板与颜色 token 解析 | 把中立错误类型转成该语言自己的异常 |
| 能力模型与路径解析 | 视图如何定义——`class extends View`，还是元表 |
| 长度与颜色的强制转换 | 沙箱中与语言相关的部分 |
| 中立错误类型、回调 arena、错误覆盖层 | |
| `ScriptView`、`ShellRoot`、热重载 | |

左侧的模块，源码里没有一处出现 VM 的名字。这才是接缝为真的证据：它不是一个 trait，而是"crate 的其余部分只通过十来个入口触达引擎、此外别无他途"这一事实。

在这里用 trait 反而更糟。两个句柄类型——视图类与视图实例——在 QuickJS 一侧各自带着生命周期标注，硬套一层 trait 只会把这份复杂度搬进类型系统，而不会消掉它。

## 可移植性

**脚本在两个引擎之间不可移植。** 它们是不同的语言：视图在 JavaScript 里是 `class Counter extends View`，在 Lua 里是一张元表。

在两边**相同**的是其余一切——绑定接口、渲染协议、phase 规则、能力模型、错误信息。设计提出的要求是行为层面的：同一个用例在两个引擎下必须产出**同一棵描述树**。这正是防止接缝腐烂成两个各行其是的运行时的东西。

## 已知缺口：异步还没有完全落在接缝之上

接缝的契约目前不覆盖异步，这是一个真实的洞而不是疏漏。

Lua 的 coroutine 与 JavaScript 的 promise 不是同一种机制，而 QuickJS 还额外要求宿主自己去清空 job 队列——没人来问，`await` 之后的代码就永远不执行。所以调度器无法整体落在接缝之上。它需要每个引擎再提供两个操作：把一个宿主任务变成脚本可等待的值，以及跑完待执行的 job（在 Lua 一侧是空操作）。

在这两条补进契约之前，调度器是 QuickJS 专属的。它将来要遵守的规则，与任何新增能力的规则一样：除非确实无法表达，否则加在接缝之上；如果确实必须落在下面，**两个引擎都要实现，或者缺失的一侧抛出明确异常**。一个引擎有、另一个静默无行为，正是这类接缝腐烂的方式。

## 为什么不用 WebAssembly，也不用独立进程

接缝会引出的两个问题。

`gpui-shell` 把 VM 跑在**宿主进程内、主线程上**，与 GPUI 的 `App` 在一起。这不是图省事——正是它才让几百纳秒量级的单次调用成本成为可能。独立进程会在每帧重建元素树的路径上加一次 IPC 往返，而这里没有那份预算。同样的理由也解释了为什么没有 `Worker`：VM 与 `App` 都是主线程独占的。

wasm 目标是接缝画在这个位置的另一个理由。QuickJS 是纯 C，能编到 WebAssembly；LuaJIT 不能。LuaJIT 还会生成机器码，这在禁止可写可执行内存的平台上是一项约束。这些事实都不决定今天的默认，但它们是"引擎是架构的一个参数，而不是架构的一部分"被写下来而不是被假定的原因。
