/// Placeholder for the tab-group entity that owns a panel's active-tab state.
/// Filled in by Task 12; `Panel::on_added_to` and `PanelView::on_added_to`
/// already reference it so their signatures do not change again once it
/// lands.
pub struct TabGroup {
    _private: (),
}
